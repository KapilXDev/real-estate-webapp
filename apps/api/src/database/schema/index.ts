/**
 * Drizzle schema.
 *
 * ⚠️ MIRRORS the hand-written SQL in `../migrations/`. It does not generate it.
 * Change the SQL first, then reflect it here. `drizzle-kit generate` must not be run against
 * this schema — see BUILD_LOG step 9 for the reasoning.
 */

export * from "./enums";
export * from "./identity";
