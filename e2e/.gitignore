# Saved staff session. A real (if local) credential — never committed.
.auth/

# Generated by global-setup rather than checked in; see the note there.
fixtures/*.jpg

# Playwright output.
test-results/
playwright-report/
blob-report/
